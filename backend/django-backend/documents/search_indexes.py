# from django_elasticsearch_dsl import Document, Index
# from .models import Document as DocModel

# documents_index = Index('documents')  # Make sure this is not empty

# @documents_index.document
# class DocumentDocument(Document):
#     class Django:
#         model = DocModel
#         fields = ['title', 'language', 'processed_text']
